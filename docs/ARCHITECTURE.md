# AstraSecure Architecture

- Monorepo with backend (Node.js/Express), frontend (React), AI microservice (Python/Flask)
- PostgreSQL and MongoDB for data storage
- Deployed via Docker Compose, AWS scripts provided